@model IEnumerable<ExwhyzeeEDI.Web.Models.Entities.Post>


@foreach (var item in Model)
{
    <li><a href="#">@item.PostTite</a></li>

    @*<article class="post media-post clearfix pb-0 mb-10">
        <a href="@Url.Action("Details", "Blog", new { id = item.Id })" class="post-thumb">

            @if (!String.IsNullOrEmpty(item.ImageUrl))
            {
                <img alt="" src="~/Uploads/@item.ImageUrl" style="max-height:80px;max-width:55px;">

            }
        </a>
        <div class="post-right">
            <h5 class="post-title mt-0 mb-5"><a href="@Url.Action("Details", "Blog", new { id = item.Id })">@item.PostTite</a></h5>
            <p class="post-date mb-0 font-12">@item.DatePosted.ToString("MM dd, yyyy") </p>
        </div>
    </article>*@
   
}